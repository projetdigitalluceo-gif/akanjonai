# Static site — no build step, publish the repository root as-is.
[build]
  publish = "."

# Long-cache the static assets (images, fonts) for performance.
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
